# mdview
simple markdown renderer !

# using
make sure you have the rust toolchain installed <br>

`cargo run --release -- /path/to/md`

go to `http://localhost:3030` and you will find your markdown!
